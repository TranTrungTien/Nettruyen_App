const APP_NAME = 'Minh Nguyệt Truyện';

enum StatusComic { all, completed, ongoing }

enum TopType { all, daily, monthly, chapter, follow, weekly, comment }

enum Status { loading, succesfull, error }
